{{#if hbsObject.tickets.latest.createdAt}}
<div class="item">
  <div class="wrapper">
    <div class="image">
      <h3>
        <a href="#" class="title">tickets</a>
      </h3>
      <a href="#" class="image-wrapper background-image">
          <img src="{{hbsObject.tickets.latest.image}}" alt="">
      </a>
    </div>
    <div class="price">${{hbsObject.tickets.latest.price}}</div>
    <div class="meta">
      <figure>
        <i class="fa fa-calendar-o"></i>{{momentFromNowTime hbsObject.tickets.latest.createdAt}}
      </figure>
      <figure>
        <a href="#">
          <i class="fa fa-user"></i>{{hbsObject.tickets.latest.email}}
        </a>
      </figure>
    </div>
    <div class="description">
      <p>{{hbsObject.tickets.latest.details}}</p>
    </div>
    <div class="additional-info">
      <ul>
        <li>
          <figure>Latest Post</figure>
          <aside>{{momentFromNowTime hbsObject.tickets.latest.createdAt}}</aside>
        </li>
        <li>
          <figure>Total number of tickets listed</figure>
          <aside>{{hbsObject.tickets.classifiedCount}}</aside>
        </li>
      </ul>
    </div>
  </div>
</div>
{{/if}}
